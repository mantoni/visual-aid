{
  "name": "visual-aid",
  "version": "0.1.0",
  "private": true,
  "description": "A Tauri visual aid that coding agents can launch and send structured data to over MCP.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "mcp": "tsx mcp/server.ts",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "keywords": [
    "tauri",
    "mcp",
    "visualization",
    "agents"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "*",
    "@tauri-apps/api": "^2.10.1",
    "zod": "*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "*",
    "@types/node": "*",
    "tsx": "*",
    "typescript": "*",
    "vite": "*"
  }
}
